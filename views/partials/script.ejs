var xhttp = new XMLHttpRequest();
xhttp.onreadystatechange = function(){
if(this.readyState == 4 && this.status == 200){
myFunction(this);
}
};
xhttp.open("GET", "/articles", true);
xhttp.send();
function myFunction(xml) {
var xmlDoc = xml.responseXML;
var target = xmlDoc.getElementsByTagName("target");
var itemCat = xmlDoc.getElementsByTagName("category");
var title = xmlDoc.getElementsByTagName("title");
var information = xmlDoc.getElementsByTagName("text");
var article = 0;
var articleNum = 0;
for(i = 0; i < title.length; i++){
if(itemCat[i].childNodes[0].nodeValue == category && category == "Events"){
  document.getElementById("event-slidershow").innerHTML += "<div class='mySlides' id='event" + article + "'><h2>" + title[i].childNodes[0].nodeValue + "</h2><p>" + information[i].childNodes[0].nodeValue + "</p></div>";
  article += 1;
  articleNum += 1;
}else if(itemCat[i].childNodes[0].nodeValue == category || category == ""){
document.getElementById(target[i].childNodes[0].nodeValue).innerHTML += "<p>" + information[i].childNodes[0].nodeValue + "</p>";
document.getElementById(target[i].childNodes[0].nodeValue + "-other").innerHTML += "<h2>" + title[i].childNodes[0].nodeValue + "</h2>";
}
}
}
